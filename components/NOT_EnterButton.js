import { Button } from "react-native"


export default function EnterButton() {
    return (
    <Button 
    
    title="sumbit"/>
    )
}

// button component in React Native does not accept a style prop, so we will use a Pressable in the InputBar file to adjust the width & height